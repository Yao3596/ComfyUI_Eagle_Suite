# Eagle Suite 核心依赖
imageio_ffmpeg>=0.4.9
opencv-python>=4.8.0
requests>=2.31.0
Pillow>=10.0.0
numpy>=1.24.0
aiohttp>=3.8.0
av>=10.0.0

# 音频元数据、读写和重采样
soundfile>=0.12.0
mutagen>=1.47.0
scipy>=1.10.0
keyring>=24.0.0
cryptography>=41.0.0

# Danbooru 标签语义搜索
jieba>=0.42.1
pandas>=1.5.0
pyarrow>=10.0.0
safetensors>=0.4.0
sentence-transformers>=2.3.0

# Hugging Face 缓存定位（Danbooru 本地语义模型）
huggingface_hub>=0.20.0

# 本地大模型节点（不使用本地模型时可不安装）
transformers>=4.35.0
accelerate>=0.24.0

# 可选：本地大模型节点加载 GGUF 量化模型（llama.cpp 模式）。
# 未安装时加载器会明确报错，不阻塞 ComfyUI 启动；可参考 ComfyUI TE 整合包安装对应 wheel。
# llama-cpp-python>=0.2.0

# torch、safetensors、folder_paths 和 comfy 由 ComfyUI 环境提供，请勿在此重复固定版本。
